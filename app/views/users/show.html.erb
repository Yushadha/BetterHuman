<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>

<% @goal.each do |goal| %>
	<%= goal.ranking %>
	<%= goal.description %>
	<br>
<% end %>

<%=  form_for @new_goal do |new_goal| %>
	<%= new_goal.text_area :description, :value => "my next goal" %>
	<%= new_goal.submit 'add goal' %>
<% end %>
